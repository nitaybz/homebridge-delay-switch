{
    "pluginAlias": "DelaySwitch",
    "pluginType": "accessory",
    "singular": false,
    "headerDisplay": "Homebridge plugin for creating delay/timer switches and sensors for delayed actions/automations",
    "footerDisplay": "Created by @nitaybz",
    "schema": {
        "type": "object",
        "properties": {
            "name": {
                "title": "Accessory Name",
                "description": "Name of the accessory",
                "type": "string",
                "required": true
            },
            "delay": {
                "title": "Delay Time in Milliseconds",
                "description": "Amount of time in milliseconds to wait from when the switch is turned ON until the switch will be turned OFF and the sensor(s) will trigger",
                "type": "integer",
                "default": 5000,
                "required": true
            },

            "sensorMode": {
                "title": "Sensor Type and Mode",
                "description": "Select the desired sensor type and operating mode",
                "type": "integer",
                "oneOf": [
                    { "title": "None", "enum": [0] },
                    { "title": "Motion Sensor, activated at end of delay for 3s", "enum": [1] },
                    { "title": "Contact Sensor, opened at end of delay for 3s", "enum": [2] },
                    { "title": "Contact Sensor, closed at end of delay for 3s", "enum": [3] },
                    { "title": "Contact Sensor, opened while delay switch is on", "enum": [4] },
                    { "title": "Contact Sensor, closed while delay switch is on", "enum": [5] },
                    { "title": "Occupancy Sensor, occupied while delay switch is on", "enum": [6] },
                    { "title": "Occupancy Sensor, occupied while delay switch is off", "enum": [7] }
                ],
                "default": 0,
                "required": true
            },

            "startOnReboot": {
                "title": "Turn ON when Homebridge Restarts",
                "description": "When Enabled, the switch will be turned ON and start the timer when the Homebridge server restarts",
                "type": "boolean",
                "default": false,
                "required": false
            }
        }
    }
  }